# 맥북 24h RD 자동화 전용 (Intel 맥 / Python 3.13~3.14)
# 제외: torch / sentence-transformers / ollama / google-generativeai
#   → 이미지매칭·로컬AI용. RD(키워드·낙찰가·번역·환율)엔 불필요하고 전부 lazy import 라 안 깔아도 백엔드 정상 기동.
# 컴파일/ Rust 회피 위해 컴파일 패키지(pydantic-core·asyncpg·greenlet 등)는 휠 있는 버전으로 완화.

fastapi==0.115.0
uvicorn[standard]==0.30.0
sqlalchemy[asyncio]>=2.0.36,<2.1
aiosqlite==0.20.0
asyncpg>=0.30
python-dotenv==1.0.1
playwright>=1.49
pydantic>=2.10,<3
pydantic-settings>=2.6
sse-starlette==2.1.0
httpx==0.27.0
openpyxl==3.1.5
Pillow>=11
numpy>=2.1
